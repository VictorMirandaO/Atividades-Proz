DROP TABLE IF EXISTS Emprestimo;
DROP TABLE IF EXISTS Livro;
DROP TABLE IF EXISTS Usuarios;

CREATE TABLE Usuarios (
    Id_usuario INT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL
 );
 
 CREATE TABLE Livro (
     Id_livro INT PRIMARY KEY,
     titulo VARCHAR(100) NOT NULL,
     autor VARCHAR(100),
     ano INT
);

CREATE TABLE Emprestimo (
    id_emprestimo INT PRIMARY KEY,
    id_usuario INT,
    id_livro INT,
    data_emprestimo DATE,
    data_devolucao DATE,
    FOREIGN KEY (id_usuario) REFERENCES Usuarios(id_usuario),
    FOREIGN KEY (id_livro) REFERENCES Livro(id_livro)
);

INSERT INTO Usuarios VALUES (1, 'Maria Silva');

INSERT INTO Livro VALUES (1, 'Banco de Dados I', 'Carlos Souza', 2020);
INSERT INTO Livro VALUES (2, 'Algoritmos', 'Ana Lima', 2019);
INSERT INTO Livro VALUES (3, 'Inteligência Artificial', 'Pedro Santos', 2022);

INSERT INTO Emprestimo VALUES (1, 1, 1, '2026-05-01', '2026-05-10');
INSERT INTO Emprestimo VALUES (2, 1, 2, '2026-05-02', '2026-05-12');
INSERT INTO Emprestimo VALUES (3, 1, 3, '2026-05-03', '2026-05-11');

SELECT * FROM Emprestimo;
    
